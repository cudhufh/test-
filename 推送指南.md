# 🚀 简历仓库推送指南

你的 Git 仓库已初始化，SSH 密钥已生成。按以下步骤推送到远程仓库。

---

## 第一步：选择代码托管平台

推荐国内用户用 **Gitee（码云）**，GitHub 也可以。

| 平台 | 网址 | 特点 |
|------|------|------|
| Gitee | https://gitee.com | 国内速度快，支持QQ号登录 |
| GitHub | https://github.com | 国际通用，程序员标配 |

---

## 第二步：添加 SSH 公钥到平台

### 你的 SSH 公钥已保存在 `ssh_public_key.txt`，内容如下：

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOlIHzjtv+pXVJvouJp0nDF+vK4jswlZA5KdPlmTDAo7 2265178346@qq.com
```

### 添加到 Gitee：
1. 登录 https://gitee.com → 右上角头像 → **设置**
2. 左侧菜单 → **SSH 公钥**
3. 标题填：`laptop-resume`
4. 公钥内容：复制上面那串完整文字，粘贴进去
5. 点击 **确定**，输入密码确认

### 添加到 GitHub：
1. 登录 https://github.com → 右上角头像 → **Settings**
2. 左侧菜单 → **SSH and GPG keys** → **New SSH key**
3. Title 填：`laptop-resume`
4. Key：复制上面那串完整文字，粘贴进去
5. 点击 **Add SSH key**

---

## 第三步：在平台上创建空仓库

### Gitee：
1. 点击右上角 `+` → **新建仓库**
2. 仓库名称：`jianli`（或你喜欢的名字）
3. **是否开源**：建议选"私有"（简历属于个人隐私）
4. **初始化仓库**：**不要勾选** "初始化仓库"（README、.gitignore 都不要选）
5. 点击 **创建**

### GitHub：
1. 点击右上角 `+` → **New repository**
2. Repository name：`jianli`
3. **Privacy**：选 **Private**
4. **Initialize**：**不要勾选** "Add a README file"
5. 点击 **Create repository**

---

## 第四步：推送代码

### 方法一：运行一键脚本（推荐）

创建完成后，平台会显示类似这样的地址：

```
# Gitee 地址示例：
git@gitee.com:你的用户名/jianli.git

# GitHub 地址示例：
git@github.com:你的用户名/jianli.git
```

**复制这个地址**，然后运行目录下的脚本：

```bash
# 如果你会用 PowerShell：
.\一键推送.ps1

# 或者在文件资源管理器里双击运行 "一键推送.bat"
```

按提示粘贴仓库地址，脚本会自动完成推送。

### 方法二：手动命令推送

```bash
# 进入目录
cd d:\jianli

# 添加远程仓库（把下面的地址换成你自己的！）
# Gitee 示例：
git remote add origin git@gitee.com:你的用户名/jianli.git

# GitHub 示例：
git remote add origin git@github.com:你的用户名/jianli.git

# 推送到远程
git branch -M master
git push -u origin master
```

---

## 第五步：验证推送成功

刷新你的 Gitee/GitHub 仓库页面，看到以下三个文件即成功：

- `resume_template.md`
- `resume.html`
- `resume_editable.html`

---

## 🆘 常见问题

### Q1：推送时提示 "Permission denied (publickey)"
**原因**：SSH 公钥没有正确添加到平台，或者添加后没有生效。

**解决**：
```bash
# 测试 SSH 连接（GitHub）
ssh -T git@github.com

# 测试 SSH 连接（Gitee）
ssh -T git@gitee.com
```

看到 `Hi xxx! You've successfully authenticated` 就表示成功了。

如果失败，检查：
1. 公钥是否完整复制（不要多/少字符）
2. 是否使用了正确的 SSH 地址（不是 HTTPS 地址）

---

### Q2：推送时提示 "fatal: Could not read from remote repository"
**原因**：远程仓库地址写错了。

**解决**：检查地址格式：
```bash
git remote -v
```
确保是 `git@gitee.com:用户名/仓库名.git` 这种格式。

---

### Q3：我想用 HTTPS 而不是 SSH
如果你不想配置 SSH，可以用 HTTPS 地址推送，但每次需要输入用户名密码：

```bash
# Gitee HTTPS 示例：
git remote add origin https://gitee.com/你的用户名/jianli.git

# GitHub HTTPS 示例：
git remote add origin https://github.com/你的用户名/jianli.git
```

---

## 📁 当前仓库文件说明

| 文件 | 说明 |
|------|------|
| `resume_template.md` | Markdown 简历模板（含撰写指南） |
| `resume.html` | 精美排版版简历（浏览器打开） |
| `resume_editable.html` | **可编辑版**简历（浏览器里直接点击修改） |
| `ssh_public_key.txt` | 你的 SSH 公钥（用于添加到 Gitee/GitHub） |
| `推送指南.md` | 本文件 |
| `一键推送.bat` / `一键推送.ps1` | 推送辅助脚本 |

---

> 💡 **提示**：简历属于个人隐私，建议将远程仓库设为**私有（Private）**。
